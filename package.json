{
  "name": "aoughwl-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Docs site for aoughwl — the self-hosted Nimony toolchain",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "claims": "python3 tools/claimcheck.py",
    "claims:all": "python3 tools/claimcheck.py --all",
    "claims:sweep": "python3 tools/claimcheck.py --sweep",
    "claims:list": "python3 tools/claimcheck.py --list"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
