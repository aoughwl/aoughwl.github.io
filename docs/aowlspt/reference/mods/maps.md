<!-- GENERATED by tools/gendocs.py in the aowlspt repo. DO NOT EDIT BY HAND. -->
<!-- Re-run: python tools/gendocs.py --out <site>/docs/aowlspt/reference -->

# `maps`

Source: [`mods/maps`](https://github.com/aoughwl/aowlspt/blob/main/mods/maps).

## Settings

Defined in [`mods/maps/config.json`](https://github.com/aoughwl/aowlspt/blob/main/mods/maps/config.json). The values shown are the **shipped defaults**, not what is currently deployed.

### `enabled`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `profile`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `hz`

- **Default:** `10`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `radiusM`

- **Default:** `120`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `hudRadar`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `hudIndicators`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `radarSize`

- **Default:** `220`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `radarX`

- **Default:** `24`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `radarY`

- **Default:** `24`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `showBots`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `showPlayers`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `showLocal`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `showPmc`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `showScav`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `showBoss`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `showUnknown`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `colorLocal`

- **Default:** `"FFFFFF"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `colorPmc`

- **Default:** `"46BEFF"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `colorScav`

- **Default:** `"E6463C"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `colorBoss`

- **Default:** `"FF3CDC"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `colorUnknown`

- **Default:** `"969696"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `screenW`

- **Default:** `1920`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `screenH`

- **Default:** `1080`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `hudBudgetUs`

- **Default:** `400`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `hudMap`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `mapX`

- **Default:** `320`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `mapY`

- **Default:** `120`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `mapSize`

- **Default:** `720`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `mapSpanM`

- **Default:** `500`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `mapGrid`

- **Default:** `8`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `indicatorInset`

- **Default:** `48`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `indicatorHoldMs`

- **Default:** `400`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `indicatorFadeMs`

- **Default:** `1600`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `indicatorGraceMs`

- **Default:** `1200`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `mapsRenderer`

- **Default:** `"unified"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `spatialMode`

- **Default:** `"auto"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `zoom`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `opacity`

- **Default:** `0.85`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `drawArt`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `contactSize`

- **Default:** `2.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `colorBot`

- **Default:** `"E6463C"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `colorPlayer`

- **Default:** `"46BEFF"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `widgetAnchor`

- **Default:** `"manual"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `widgetBorder`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `northPip`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `widgetBorderPx`

- **Default:** `2.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `widgetBackdropOpacity`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `hotkeys`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fullscreenKey`

- **Default:** `"M"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fullscreenSpanM`

- **Default:** `1200`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fullscreenZoom`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fullscreenMargin`

- **Default:** `48`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fullscreenOpacity`

- **Default:** `0.92`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fullscreenNorthUp`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `heading`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `markers`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

