<!-- GENERATED by tools/gendocs.py in the aowlspt repo. DO NOT EDIT BY HAND. -->
<!-- Re-run: python tools/gendocs.py --out <site>/docs/aowlspt/reference -->

# `fov`

Source: [`mods/fov`](https://github.com/aoughwl/aowlspt/blob/main/mods/fov).

## Settings

Defined in [`mods/fov/config.json`](https://github.com/aoughwl/aowlspt/blob/main/mods/fov/config.json). The values shown are the **shipped defaults**, not what is currently deployed.

### `opticFovMulti`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `nonOpticFovMulti`

- **Default:** `1.25`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `opticCameraDistanceOffset`

- **Default:** `-0.03`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `nonOpticCameraDistanceOffset`

- **Default:** `-0.01`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolCameraDistanceOffset`

- **Default:** `0.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleLeftShoulderOffset`

- **Default:** `0.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolLeftShoulderOffset`

- **Default:** `0.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `cameraIncreaseOffsetKey`

- **Default:** `"KeypadMultiply"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `cameraDecreaseOffsetKey`

- **Default:** `"KeypadDivide"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `zoomToggleKey`

- **Default:** `"M"`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `holdToZoom`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `cancelZoomOnUnAds`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `zoomOnHoldBreath`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `opticToggleZoomMulti`

- **Default:** `0.9`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `nonOpticToggleZoomMulti`

- **Default:** `0.8`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `unaimedToggleZoomMulti`

- **Default:** `0.8`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `opticToggleZoomSensMulti`

- **Default:** `0.8`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `nonOpticToggleZoomSensMulti`

- **Default:** `0.7`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `unaimedToggleZoomSensMulti`

- **Default:** `0.7`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `changeMouseSensitivity`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `nonOpticSensMulti`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `oneSensMulti`

- **Default:** `0.75`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `twoSensMulti`

- **Default:** `0.45`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `threeSensMulti`

- **Default:** `0.3`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fourSensMulti`

- **Default:** `0.2`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fiveSensMulti`

- **Default:** `0.15`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `sixSensMulti`

- **Default:** `0.125`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `eightSensMulti`

- **Default:** `0.08`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `tenSensMulti`

- **Default:** `0.04`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `twelveSensMulti`

- **Default:** `0.03`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `highSensMulti`

- **Default:** `0.01`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleCameraXOffset`

- **Default:** `0.04`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleCameraYOffset`

- **Default:** `0.04`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleCameraZOffset`

- **Default:** `0.025`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolCameraXOffset`

- **Default:** `0.04`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolCameraYOffset`

- **Default:** `0.04`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolCameraZOffset`

- **Default:** `0.025`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `enableFovScaleFix`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `fovScale`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `maxBaseFov`

- **Default:** `110`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `minBaseFov`

- **Default:** `30`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleCameraSpeed`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolCameraSpeed`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `opticCameraSpeed`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleAimSpeedX`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleAimSpeedY`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `rifleAimSpeedZ`

- **Default:** `3.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolAimSpeedX`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolAimSpeedY`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `pistolAimSpeedZ`

- **Default:** `1.0`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `unAimSpeedX`

- **Default:** `5.5`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `unAimSpeedY`

- **Default:** `4.5`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `unAimSpeedZ`

- **Default:** `4.5`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `enableAdsSmoothing`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `adsSmoothTime`

- **Default:** `0.16`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `unAdsSmoothTime`

- **Default:** `0.12`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `useAimSpeedsForFovSmoothing`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `adsSmoothLinear`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `adsDeferToGame`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `adsSettleEps`

- **Default:** `0.05`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `adsObserveCap`

- **Default:** `1.5`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `adsMaxFights`

- **Default:** `3`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `adsWriteFovField`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `enableFovWrite`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `verifyHooksByName`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `deferUntilUnityThread`

- **Default:** `true`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `hookFrameDriver`

- **Default:** `false`

_UNKNOWN — this key carries no doc comment in `config.json`._

### `selfTestRuntime`

- **Default:** `""`

A GameAssembly.dll for the `--side sim` self-test to bind against, so it reports real bindings offline. Empty -- which is what ships -- means the process, and under aowlspt-sim there is no runtime at all. It must be an ABSOLUTE path: a relative one resolves against whatever directory the host was started in, which in an install is the game's own directory, and it is refused with that reason. `aowl test` and `aowl run` set AOWLSPT_SELFTEST_RUNTIME to the stand-in runtime tests/mockil2cpp builds, which is why this key is empty here and nothing ships a fixture path into an install.

### `selfTestDataDir`

- **Default:** `""`

The *_Data directory il2cpp_init is given when selfTestRuntime was loaded out of process -- a real client's EscapeFromTarkov_Data. Empty means the runtime is started without one, which is right for the stand-in and wrong for a real GameAssembly.dll. Absolute paths only, same rule and same reason as selfTestRuntime; AOWLSPT_SELFTEST_DATADIR sets it for a whole run. The mod has read this key since the self-test learned to start a runtime, so it is declared here where it can be found.

